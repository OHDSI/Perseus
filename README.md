Prepare xml for CDMBuilder based on json  through POST service

In order to RUN API

1. Check path to the configuration/default.json
2. Check rest_api.py for existance:

if __name__ == '__main__':
    app.run(host='0.0.0.0')
	